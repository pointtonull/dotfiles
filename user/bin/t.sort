#!/usr/bin/awk -f

/^\[[xf]\]/{done[d++]=$0; next}
/^\[ \]/{pending[p++]=$0; next}
/^\[.\]/{other[o++]=$0; next}

END{
    for (o in other) print other[o]
    for (p in pending) print pending[p]
    for (d in done) print done[d]
}
