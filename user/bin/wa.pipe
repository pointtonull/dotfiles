#!/bin/sh

orig=$1 #milva, ana
dest=$2 #numero destino

awk -v orig="$orig" -v dest="$2" '

    NR == 1{
        message = $0
    }

    NR > 1{
        message = message "\n" $0
    }

    END{
        system("yowsup-cli -c ~/.wa." orig " -ws " dest " \"" message "\" > /dev/null")
    }

'
